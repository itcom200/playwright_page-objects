version: "3.9"

services:
  playwright:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: playwright_docker
    working_dir: /app
    volumes:
      - .:/app
    command: ["npx", "playwright", "test"]
